# Env / secrets — ignore real env files but keep the templates checked in.
.env
.env.*
!.env.example
!.env.deploy.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# uv
# (uv.lock IS committed)

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Docker
.docker/

# Local-only working assets (logos / screenshots not yet wired into the README).
# Force-add (`git add -f images/foo.jpg`) when you're ready to commit one.
images/
